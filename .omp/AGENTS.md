# Portfolio OMP Appendix

Read the repository root `AGENTS.md` first; it is the canonical project instruction file.

- Use `.agents/envelope/` for tracker, domain, command, and template facts.
- Read `docs/agents/triage-labels.md`, `docs/agents/domain.md`, and `docs/agents/scope-ledger.md` when relevant.
- Use matching project skills from `.agents/skills/`.
- Keep the active preview-stack and one-issue/one-worktree rules defined in root `AGENTS.md`.
- Treat this file as an OMP-specific pointer only; do not duplicate the project workflow here.
