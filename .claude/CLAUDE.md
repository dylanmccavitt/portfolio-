# Portfolio

This file intentionally defers to the canonical agent entry point.

**Read `AGENTS.md` at the repo root first** — it holds the stack, constraints,
workflow, and the `## Agent skills` block that points at the per-repo envelope.

The single source of truth for repo workflow facts is the envelope:

- `.agents/envelope/linear-map.md` — Linear team (`dmcc`/AGE) + Portfolio project, labels, states, triage map, GitHub bridge.
- `.agents/envelope/domain.md` — domain glossary (DM, Eve retired in AGE-818, agent-first portfolio) — authoritative product/design direction.
- `.agents/envelope/commands.md` — build/test/lint/run + branches.
- `.agents/envelope/templates/` — PR / issue / project-doc templates.

Continuity for the agent-first redesign lives in `docs/agents/scope-ledger.md`.
