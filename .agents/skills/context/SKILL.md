---
name: context
description: Always-load-first context skill. Reads current project state and orients the session.
---

# Context

Load this skill first for every task. Orients the session with current state.

## Scope Guard

- Apply only in the `portfolio` project.

## Read This First

1. `AGENTS.md` (root) and `.omp/AGENTS.md` — project goals, constraints, and workflow
2. `.agents/envelope/` — Linear map, domain glossary, commands (authoritative repo facts)
3. `git status` / `git log --oneline -5` — recent changes
4. Root directory structure — what exists so far
